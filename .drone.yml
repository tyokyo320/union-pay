---
kind: pipeline
type: docker
name: union-pay

steps:
- name: ssh commands
  image: appleboy/drone-ssh
  settings:
    host: 172.17.0.1
    username: zhangqiang
    key:
      from_secret: id_rsa
    port: 22
    script:
      - docker-compose -f docker/union-pay/docker-compose.yml down -v
      - rm -rf docker/union-pay
      - git clone https://github.com/tyokyo320/union-pay.git docker/union-pay
      - docker-compose -f docker/union-pay/docker-compose.yml -d --build



trigger:
  event:
  - push
